server:
  port: 8080

spring:
  application:
    name: points-exchange-system

  # === MySQL 配置 (支持环境变量部署) ===
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://113.44.121.131:3306/points_exchange?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}
    username: ${SPRING_DATASOURCE_USERNAME:rsdb}
    password: ${SPRING_DATASOURCE_PASSWORD:rs@2025}
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Shanghai
    open-in-view: false

  # === H2 配置 (备用) ===
  # datasource:
  #   url: jdbc:h2:file:./data/points-exchange;MODE=MYSQL;DB_CLOSE_DELAY=-1
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password:
  # jpa:
  #   database-platform: org.hibernate.dialect.H2Dialect
  #   hibernate:
  #     ddl-auto: update
  #   properties:
  #     hibernate:
  #       format_sql: true
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

app:
  legacy-static-path: ./legacy-static
  seed-admin-username: admin
  seed-admin-password: admin123
  seed-user-username: demo
  seed-user-password: demo123
  seed-user-phone: 13800000000
  sms-mock-enabled: true
  sms-code-ttl-seconds: 300
  sms-resend-interval-seconds: 60
  aliyun-sms-access-key-id: YOUR_ACCESS_KEY_ID
  aliyun-sms-access-key-secret: YOUR_ACCESS_KEY_SECRET
  aliyun-sms-sign-name: 四川佳宏凯原商贸
  aliyun-sms-template-code: SMS_334955643

kuaidi100:
  customer: 4F8FE114369C1F6797B8A05292B976BA
  key: EEzFumDe5683
